server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/arr331/microservices.git
          searchPaths: config-data
          username: ${GIT_USER}
          password: ${GIT_PASSWORD}
  security:
    user: root
    password: s3cr3t
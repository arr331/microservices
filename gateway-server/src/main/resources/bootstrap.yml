server:
  port: 8080

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8099/eureka/

spring:
  application:
    name: gateway-service
  security:
    user:
      name: arr
      password: 123

zuul:
  routes:
    client:
      path: /client/**
      url: http://localhost:8093/client